# Where RSVPs are forwarded as a JSON POST. Any endpoint works: a Google Apps
# Script web app bound to a Sheet, Zapier, Formspree, a Slack incoming webhook.
# Leave unset and RSVPs are only logged to the server console.
RSVP_WEBHOOK_URL=
